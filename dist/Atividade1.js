"use strict";
for (let left = 1; left <= 10; left++) {
    console.log(`Tabuada do ${left}`);
    for (let tabuada = 1; tabuada <= 10; tabuada++) {
        console.log(`${left} x ${tabuada} = ${tabuada * left}`);
    }
}
